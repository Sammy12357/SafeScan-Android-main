# Screenshots — 1080 × 1920 PNG (portrait)

Place these three files here (exact names; referenced by `../../config.yaml`):

- `scanner.png` — scanner / camera screen
- `result.png`  — risk verdict screen
- `airdrop.png` — airdrop tier screen

Capture at 1080 × 1920 from a real device or an emulator set to that resolution.
